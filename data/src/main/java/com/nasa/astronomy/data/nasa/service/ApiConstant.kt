package com.nasa.astronomy.data.nasa.service

class ApiConstant {
    companion object {
        // endpoint to get the url to download the config file from
        const val API_IMAGE_INFO = "/planetary/apod"
        const val API_PARAM_KEY = "api_key"
        const val API_PARAM_DATE = "date"
    }
}
